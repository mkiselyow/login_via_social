<% if @feed_items.any? %>
  <ul class="posts">
    <%= will_paginate @feed_items %>
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
    <%= will_paginate @feed_items %>
  </ul>
<% end %>